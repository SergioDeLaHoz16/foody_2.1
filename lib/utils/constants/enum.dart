enum OrderStatus { pending, processing, shipped, delivered, cancelled }

enum PaymentStatus { pending, success, failed }

enum PaymentMethod { paypal, card, googlePay, applePay, masterCard, creditCard }
